{
  "name": "aristocks-calculator",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "wrangler": "^4.68.0"
  }
}
